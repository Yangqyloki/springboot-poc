server:
  port: 8761
spring:
  application:
    name: eureka-server
  cloud:
    config:
      server:
        git:
          uri: https://github.wdf.sap.corp/CNACC/config-repo
          cloneOnStart: true


      #spring.application.name=eureka-server
      #
      #
      #spring.cloud.config.uri=http://localhost:10090
      #spring.cloud.config.enabled=true
      #management.endpoints.web.exposure.include=refresh